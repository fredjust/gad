MON TUTO GIT

git config --global user.name "fredjust"
git config --global user.email "fredjust@gmail.com"

liste les fichiers
ls

ajoute tous les fichier
git add .

git status

git commit -a -m "message"
git commit -am "message"

se place sur une version 
git checkout +SHA

se placer sur la derniere version
git checkout master

récupérer tout le code d'un projet depuis le début
git clone

envoyer du code en ligne
git remote

cat AUTHOURS

envoyer du code vers github
git push origin master 

récupérer du code vers github 
git pull origin master


BRANCHE 
les commits de deux branches sont indépendant

afficher la branche en cours
git branch

créer une branche
git branch mon-test

changer de branche
git checkout mon-test


fussioner la branche dans master 
en etant sur la branche master
git merge mon-test


GIT INGNORE
fichier .gitignore
dedans les fichiers a ignorer






